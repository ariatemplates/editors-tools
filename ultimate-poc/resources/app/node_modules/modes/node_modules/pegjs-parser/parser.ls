class Parser
	(parser) ~>
		if not parser?parse? => throw {
			msg: 'Invalid given parser'
			parser
		}

		@parser = parser

	parse: -> @parser.parse ... |> @post-process

	/**
	 *  To be overriden.
	 */
	post-process: -> it



exports <<< {
	Parser
}
