var ModeManager = require('./modeManager').ModeManager;



var modeManager = new ModeManager();

modeManager.add([
	{
		names: ['html', 'HTML', 'xhtml', 'XHTML'],
		mode: require('./html')
	},
	// !!! JS mode not working for now
	// {
	// 	names: ['js', 'JS', 'javascript', 'JavaScript'],
	// 	mode: require('./js')
	// },
	// !!! AT mode not working for now because of the JS mode
	// {
	// 	names: ['at', 'AT', 'ariatemplates', 'AriaTemplates'],
	// 	mode: require('./at')
	// }
]);


module.exports = modeManager;
