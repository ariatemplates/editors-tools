var fs = require('fs');

function read(path) {
	return fs.readFileSync(path, 'utf-8');
}

require('pegjs-parser/test')({
	// parser: read('grammar.pegjs'),
	// options: require('./options'),

	// path: 'test.tpl',
	// encoding: 'utf-8',
	source: read('test.tpl'),

	maxSourceLength: 500,

	// loggingMode: 'raw'
	// loggingMode: 'simplified'
	loggingMode: 'simpletree'
});
