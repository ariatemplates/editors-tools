/**
 * Builds the highlighting representation for the given graph using the given stylesheet.
 */
function highlight(tokens) {
	throw Error('unimplemented');

	var output = [];
	var token;
	for (var i = 0, length = tokens.length; i < length; i++) {
		token = tokens[i];
		output.push({

		});
	}
}

/**
 * Returns the stylesheet for the given session
 */
function stylesheet(session) {
	throw Error('unimplemented');
	return session.stylesheet;
}



exports.highlight = highlight;
exports.stylesheet = stylesheet;
