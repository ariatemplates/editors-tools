var factory = require('outline/ruleset').factory;



var rules = factory({
	'text': function(node) {
		return {
			label: node.get('value').substring(0, 10) + '...'
		}
	}
});



module.exports = rules;
